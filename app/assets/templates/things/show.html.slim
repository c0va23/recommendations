h1 Thing "{{thing.name}}" \#{{thing.id}}

div wg-twitter-button=true label="{{thing.name}}"

h2 Tags
ul
  li ng-repeat="tag in thing.tags" {{tag}}

div wg-score=true thing-id="thingId"
div wg-comments=true thing-id="thingId"
